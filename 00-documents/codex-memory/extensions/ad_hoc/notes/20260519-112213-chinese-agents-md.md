# Memory update: Chinese AGENTS.md preference

User explicitly said: "记住，以后用中文与 AGENTS.md".

Interpretation for future work:
- Continue using Chinese as the default collaboration language.
- When generating or updating `AGENTS.md`, prefer Chinese document text unless the user explicitly asks for another language.
- Keep the filename `AGENTS.md` unchanged unless the user asks for a different file.
